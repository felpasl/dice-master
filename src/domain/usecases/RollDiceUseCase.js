/**
 * RollDiceUseCase
 * Contains the business logic for rolling a dice
 */
class RollDiceUseCase {
  constructor(diceRepository) {
    this.diceRepository = diceRepository;
  }
  
  execute(sides) {
    // Business logic will be implemented here
  }
}

export default RollDiceUseCase;